
export type Image = {
    id?: string,
    url: string,
    fileName?: string,
    filePath?: string,
    version?: number,
    deleted?: boolean,
    createdAt?: Date,
    updatedAt?: Date,
}